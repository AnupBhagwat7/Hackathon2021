package com.bemyeyes.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class VisionController {
	/*
	 * 
	 * @Autowired private ResourceLoader resourceLoader;
	 * 
	 * @Autowired private CloudVisionTemplate cloudVisionTemplate;
	 * 
	 * @RequestMapping("/getFaceDetection") public String getFaceDetection() throws
	 * IOException {
	 * 
	 * Resource imageResource =
	 * this.resourceLoader.getResource("file:src/main/resources/pp9.jpg"); Resource
	 * outputImageResource =
	 * this.resourceLoader.getResource("file:src/main/resources/output.jpg");
	 * AnnotateImageResponse response = this.cloudVisionTemplate.analyzeImage(
	 * imageResource, Feature.Type.FACE_DETECTION);
	 * 
	 * writeWithFaces(imageResource.getFile().toPath(),
	 * outputImageResource.getFile().toPath(), response.getFaceAnnotationsList());
	 * 
	 * return response.getFaceAnnotationsList().toString(); }
	 * 
	 * @RequestMapping("/getFaceDetection") public String getFaceDetection() throws
	 * IOException {
	 * 
	 * Resource imageResource =
	 * this.resourceLoader.getResource("file:src/main/resources/pp9.jpg"); Resource
	 * outputImageResource =
	 * this.resourceLoader.getResource("file:src/main/resources/output.jpg");
	 * AnnotateImageResponse response = this.cloudVisionTemplate.analyzeImage(
	 * imageResource, Feature.Type.FACE_DETECTION);
	 * 
	 * writeWithFaces(imageResource.getFile().toPath(),
	 * outputImageResource.getFile().toPath(), response.getFaceAnnotationsList());
	 * 
	 * return response.getFaceAnnotationsList().toString(); }
	 */}
